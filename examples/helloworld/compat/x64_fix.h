#ifndef X64_FIX_H
#define X64_FIX_H

/* On x64 POSIX target, we use the host's pthread implementation for the minimum condition. */
#include <pthread.h>
#include <hal/esp32_wroom32.h>

#endif /* X64_FIX_H */
